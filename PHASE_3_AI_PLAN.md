# 阶段 3: AI 数据分析功能开发计划

> **状态: 🟢 主体已完成（2026-09-17）**。三个端点已上线并端到端验证通过。
> 下方勾选项已按实际代码核对；未实现项保留未勾选并标注原因。
> 与本计划的偏差（缓存用进程内 `Map` 而非 Redis、对话为单轮无状态等）见文末「实际实现偏差」。

## 📋 目标

在现有 MVP 基础上，集成 AI 能力，实现智能数据分析和洞察建议。

---

## 🎯 核心功能

### 3.1 AI 数据洞察
- **自动分析**: 基于历史数据，AI 自动发现趋势、异常和机会
- **智能建议**: 针对 GMV 下滑、转化率异常等提供优化建议
- **对比分析**: 店铺间横向对比，找出最佳实践

### 3.2 智能报告生成
- **周报/月报**: 自动生成数据总结报告
- **异常预警**: 实时监控指标异常并推送通知
- **预测分析**: 基于历史数据预测未来趋势

### 3.3 对话式查询
- **自然语言查询**: "上周哪个店铺表现最好？"
- **深度追问**: "为什么抖音店铺转化率下降了？"
- **数据解读**: 将复杂数据转化为易懂的商业洞察

---

## 🏗️ 技术方案

### 后端架构
```
AI Module
├── OpenAI Service (GPT-4o)
├── Prompt Templates
├── Data Context Builder
└── Insight Cache (Redis)
```

### 数据流
```
1. 用户触发 → 2. 获取数据上下文 → 3. 构建 Prompt 
→ 4. 调用 OpenAI API → 5. 解析响应 → 6. 返回洞察
```

### API 设计
```typescript
// 获取 AI 洞察
GET /api/v1/ai/insights?days=7

// 生成报告
POST /api/v1/ai/report
{
  type: 'weekly' | 'monthly',
  startDate: '2026-09-01',
  endDate: '2026-09-07'
}

// 对话式查询
POST /api/v1/ai/chat
{
  question: "为什么上周销售额下降了？",
  context: { tenantId, days: 7 }
}
```

---

## 📊 实现步骤

### Step 1: OpenAI 集成 (2小时) — ✅ 完成
- [x] 安装 `openai` npm 包
- [x] 创建 `AiModule` 和 `AiService`
- [x] 配置 API Key 和基础参数（`OPENAI_API_KEY` / `OPENAI_BASE_URL` / `OPENAI_MODEL`）
- [x] 实现基础对话功能
- [x] 未配置 Key 时降级：客户端为 `null`，接口返回 503，平台其余功能不受影响

### Step 2: 数据上下文构建 (1.5小时) — 🟡 部分完成
- [x] 实现 `buildDataContext()`（内联于 `AiService`，未单独抽成工具类）
- [x] 汇总核心指标 (GMV/订单/转化率/UV) + 店铺明细 + 最近 7 天日趋势
- [x] 计算环比变化（复用 `DashboardService.getSummary` 的 `gmvGrowth` / `ordersGrowth`）
- [ ] 同比变化（无去年同期数据，seed 仅 61 天）
- [ ] 识别异常数据点（未做规则化标注，目前交由模型自行从上下文判断）

### Step 3: Prompt 工程 (2小时) — ✅ 完成
- [x] 设计系统提示词（资深电商数据分析师角色）
- [x] 创建洞察分析模板（强制 `response_format: json_object` 返回结构化洞察）
- [x] 创建报告生成模板（Markdown 输出）
- [x] 创建对话查询模板

### Step 4: AI 洞察 API (2小时) — ✅ 完成
- [x] 实现 `GET /api/v1/ai/insights` 端点
- [x] 自动分析趋势和异常
- [x] 生成 3-5 条可操作建议（含 `title` / `description` / `suggestion` / `priority` / `category`）
- [x] 添加缓存机制（⚠️ 进程内 `Map`，15 分钟，非 Redis）

### Step 5: 智能报告生成 (2小时) — 🟡 部分完成
- [x] 实现 `POST /api/v1/ai/report` 端点
- [x] 生成结构化周报/月报
- [x] 支持 Markdown 格式输出（前端 `react-markdown` 渲染 + 下载 `.md`）
- [x] 入参校验：`type` 限 weekly/monthly，日期必填且 `start <= end`
- [ ] 支持 PDF 导出（可选项，未做）
- [ ] 报告落库（`AiAnalysis` 表已建但零使用，生成后无法回看）

### Step 6: 对话式查询 (1.5小时) — 🟡 部分完成
- [x] 实现 `POST /api/v1/ai/chat` 端点
- [x] 集成数据查询能力（复用同一份 `buildDataContext`）
- [x] 入参校验：问题非空且 ≤ 500 字，`days` 上限 90
- [ ] 支持多轮对话（⚠️ 后端为单轮无状态；前端展示历史但不回传，追问会丢上下文）
- [ ] 返回引用的数据源（未结构化返回，仅体现在回答正文里）

### Step 7: 前端集成 (3小时) — ✅ 完成
- [x] 数据看板添加"AI 洞察"卡片（`AiInsightsCard.tsx`）
- [x] 报告生成弹窗（`AiReportModal.tsx`，非独立页面）
- [x] AI 对话弹窗（`AiChatModal.tsx`，非独立页面）
- [x] 加载状态和错误处理（统一提取后端中文提示，503 提示未配置 Key）

### Step 8: 测试与优化 (1小时) — 🟡 部分完成
- [x] 编写 `ai.service.spec.ts`（9 用例，覆盖 503 降级与 `buildDataContext` 输出）
- [x] 端到端验证三个端点（本地 OpenAI 协议 stub，未消耗真实 Token）
- [x] 权限与入参校验矩阵验证（401 / 403 / 400 各分支）
- [x] 缓存策略（15 分钟内存缓存）
- [ ] Token 使用监控（未做）
- [ ] Prompt 准确性调优（尚未用真实 Key 做效果评估）

---

## 💡 Prompt 模板设计

### 系统提示词
```
你是一位资深的电商数据分析师，专门为电商商户提供数据洞察和优化建议。

你的职责:
1. 分析店铺销售数据，发现趋势、异常和机会
2. 提供具体、可操作的优化建议
3. 用简洁、专业的语言解释复杂的数据现象

数据背景:
- 用户运营多个电商店铺（淘宝、拼多多、抖音等）
- 核心指标: GMV、订单量、UV、转化率
- 需要对比不同店铺、不同时间段的表现

输出要求:
- 使用中文
- 结论先行，数据支撑
- 避免术语堆砌，直击业务本质
```

### 洞察分析模板
```
基于以下数据，生成 3-5 条关键洞察:

数据时间范围: {startDate} 至 {endDate}
总销售额: ¥{totalGMV} (环比 {growthRate}%)
总订单量: {totalOrders} 单
平均转化率: {avgConversion}%
日均访客: {avgUV} 人

店铺明细:
{shopDetails}

日趋势:
{dailyTrends}

请识别:
1. 最显著的趋势（上升/下降）
2. 异常数据点（突增/突降）
3. 店铺间的表现差异
4. 潜在的优化机会

每条洞察格式:
- 标题: 简短总结（10字内）
- 描述: 详细说明（50字内）
- 建议: 可操作的优化方向（30字内）
- 优先级: 高/中/低
```

---

## 🔐 安全与成本控制

### API Key 管理
- 使用环境变量存储
- 不提交到 Git 仓库
- 区分开发/生产环境

### Token 使用优化
- 缓存相同查询结果（15分钟）
- 限制单次查询的数据量
- 使用 GPT-4o-mini 降低成本

### 错误处理
- API 调用失败降级策略
- 超时处理（30秒）
- 用户友好的错误提示

---

## 📈 预期效果

### 业务价值
- **洞察速度**: 从手动分析 2 小时 → AI 自动 10 秒
- **决策支持**: 提供数据支撑的优化建议
- **异常发现**: 自动识别需要关注的数据波动

### 用户体验
- 数据看板一键获取 AI 洞察
- 自然语言查询，无需 SQL
- 自动生成周报，节省时间

---

## 🚀 扩展方向

### 近期
- [ ] 竞品数据对比分析
- [ ] 商品维度的智能推荐
- [ ] 营销活动效果评估

### 远期
- [ ] 多模态分析（图片、视频）
- [ ] 实时数据流分析
- [ ] 预测模型训练

---

## ⚠️ 实际实现偏差

| 项 | 本计划 | 实际实现 | 影响 / 后续 |
|----|--------|----------|-------------|
| 洞察缓存 | Redis | 进程内 `Map`（15 分钟） | 重启失效、多实例不共享；Redis 已有配置但无消费方 |
| 对话上下文 | 多轮会话 | 单轮无状态 | 前端展示历史但不回传，追问会丢上下文 |
| 数据上下文 | 独立 `DataContextBuilder` 类 | 内联 `AiService.buildDataContext()` | 功能等价，复用范围仅限本模块 |
| 异常识别 | 规则化标注异常点 | 交由模型从上下文自行判断 | 稳定性依赖 Prompt，无确定性保证 |
| 分析历史 | — | `AiAnalysis` 表零使用 | 报告/洞察无法回看，Token 重复消耗 |
| 同比分析 | 计算同比 | 仅环比 | seed 数据仅 61 天，无去年同期 |
| Token 监控 | 使用量监控 | 未做 | 成本不可观测 |
| 报告导出 | Markdown + PDF(可选) | 仅 Markdown | 前端支持下载 `.md` |

### 「安全与成本控制」落实情况
- [x] API Key 走环境变量，`.env` 已在 `.gitignore` 中（`.gitignore:12`）
- [x] 缓存相同查询 15 分钟
- [x] 限制单次数据量：日趋势只取最近 7 天；`days` 上限 90；问题上限 500 字；
      `max_tokens` 分别为 1500 / 2000 / 800
- [x] 默认模型 `gpt-4o-mini`
- [x] 未配置 Key 时降级为 503，不影响平台其余功能
- [ ] 区分开发/生产环境的 Key（目前单一配置）
- [ ] 显式超时设置（沿用 SDK 默认，未按计划设为 30 秒）

---

**预计总开发时间: 15 小时**  
**优先级: 高**  
**依赖: MVP v1.0 (已完成)**
**实际状态: 🟢 主体完成（2026-09-17），剩余项见上方「实际实现偏差」**
