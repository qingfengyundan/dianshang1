# 电商智能运营中台 - 开发规范文档 v1.0

## 文档信息

- **项目名称**: 电商智能运营中台
- **版本**: v1.0
- **最后更新**: 2026-09-16
- **适用范围**: 所有开发人员
- **状态**: 正式生效

---

## 目录

1. [Git 版本管理规范](#一git-版本管理规范)
2. [代码规范](#二代码规范)
3. [开发日志规范](#三开发日志规范)
4. [数据库变更管理](#四数据库变更管理)
5. [测试规范](#五测试规范)
6. [Code Review 规范](#六code-review-规范)
7. [CI/CD 流程](#七cicd-流程)
8. [备份与恢复策略](#八备份与恢复策略)
9. [文档规范](#九文档规范)
10. [开发规范检查清单](#十开发规范检查清单)

---

## 一、Git 版本管理规范

### 1.1 分支策略 (Git Flow)

```
main (生产环境)
  ↑
release/* (预发布)
  ↑
develop (开发主分支)
  ↑
├── feature/* (功能开发)
├── bugfix/* (bug修复)
├── hotfix/* (紧急修复)
└── refactor/* (代码重构)
```

#### 分支命名规则

```bash
# 功能开发
feature/模块名-功能描述
例: feature/dashboard-ai-analysis

# Bug 修复
bugfix/issue编号-问题描述
例: bugfix/#123-fix-price-calculation

# 紧急修复（直接从 main 拉取）
hotfix/紧急问题描述
例: hotfix/sql-injection-vulnerability

# 重构
refactor/重构模块名
例: refactor/agent-connector

# 发布分支
release/版本号
例: release/v1.0.0
```

### 1.2 Commit 提交规范 (Angular 风格)

#### 提交格式

```bash
<type>(<scope>): <subject>

<body>

<footer>
```

#### Type 类型

| Type | 说明 |
|------|------|
| feat | 新功能 |
| fix | Bug 修复 |
| docs | 文档更新 |
| style | 代码格式（不影响功能） |
| refactor | 重构（既不是新增功能，也不是修复bug） |
| perf | 性能优化 |
| test | 测试相关 |
| chore | 构建过程或辅助工具的变动 |
| revert | 回滚之前的 commit |

#### Scope 范围（模块）

| Scope | 说明 |
|-------|------|
| dashboard | 数据看板 |
| skill | 知识库 |
| agent | 智能体控制 |
| design | 设计工作台 |
| competitor | 竞品监测 |
| auth | 认证授权 |
| api | API 接口 |
| db | 数据库 |
| config | 配置 |

#### 提交示例

```bash
# ✅ 好的提交
feat(dashboard): 添加 AI 分析建议卡片显示功能

实现了数据看板中 AI 分析建议的展示，包括：
- 建议卡片 UI 组件
- 优先级颜色标识
- 执行按钮交互

Closes #45

# ❌ 不好的提交
git commit -m "修改了一些代码"
git commit -m "update"
git commit -m "fix bug"
```

### 1.3 Git 工作流程

```bash
# 1. 从 develop 创建功能分支
git checkout develop
git pull origin develop
git checkout -b feature/dashboard-ai-analysis

# 2. 开发过程中频繁提交
git add .
git commit -m "feat(dashboard): 添加 AI 建议卡片组件"
git commit -m "feat(dashboard): 实现建议数据加载逻辑"

# 3. 推送到远程（每天至少一次）
git push origin feature/dashboard-ai-analysis

# 4. 完成后创建 Pull Request
# 在 GitHub/GitLab 上创建 PR: feature/dashboard-ai-analysis → develop

# 5. Code Review 通过后合并
# 使用 Squash and Merge 或 Merge Commit

# 6. 删除本地和远程分支
git branch -d feature/dashboard-ai-analysis
git push origin --delete feature/dashboard-ai-analysis
```

### 1.4 合并策略

```bash
# develop 和 feature 分支: Merge Commit
git merge --no-ff feature/dashboard-ai-analysis

# release → main: Merge Commit + Tag
git checkout main
git merge --no-ff release/v1.0.0
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin main --tags

# hotfix → main 和 develop
git checkout main
git merge --no-ff hotfix/critical-bug
git checkout develop
git merge --no-ff hotfix/critical-bug
```

### 1.5 防止回滚机制

#### 保护分支规则（GitHub/GitLab）

在仓库设置中配置：

**main 分支保护**:
- ✅ 要求 Pull Request 审查（至少 2 人）
- ✅ 要求状态检查通过（CI/CD）
- ✅ 禁止强制推送
- ✅ 禁止删除分支
- ✅ 只允许技术负责人直接推送（紧急情况）

**develop 分支保护**:
- ✅ 要求 Pull Request 审查（至少 1 人）
- ✅ 要求状态检查通过
- ✅ 禁止强制推送

#### Git Hooks 防护

创建 `.git/hooks/pre-push` 脚本：

```bash
#!/bin/bash

# 防止直接推送到 main 和 develop
current_branch=$(git symbolic-ref HEAD | sed -e 's,.*/\(.*\),\1,')

if [ "$current_branch" = "main" ] || [ "$current_branch" = "develop" ]; then
    echo "❌ 错误: 不允许直接推送到 $current_branch 分支"
    echo "请创建 Pull Request 进行合并"
    exit 1
fi

echo "✅ 推送检查通过"
exit 0
```

赋予执行权限：
```bash
chmod +x .git/hooks/pre-push
```

### 1.6 分支管理最佳实践

1. **feature 分支生命周期**: 不超过 3 天，及时合并
2. **分支命名清晰**: 一看就知道做什么
3. **一个分支一个功能**: 不要在一个分支里做多个不相关的功能
4. **及时删除已合并的分支**: 保持仓库整洁
5. **定期同步 develop**: 避免冲突积累

---

## 二、代码规范

### 2.1 目录结构规范

```
project-root/
├── frontend/                      # 前端项目
│   ├── src/
│   │   ├── pages/                 # 页面组件
│   │   │   ├── system/            # 系统后台页面
│   │   │   │   ├── MerchantManagement/
│   │   │   │   └── SkillLibrary/
│   │   │   └── merchant/          # 商家后台页面
│   │   │       ├── Dashboard/
│   │   │       ├── MySkills/
│   │   │       ├── AgentControl/
│   │   │       ├── DesignStudio/
│   │   │       └── CompetitorMonitor/
│   │   ├── components/            # 公共组件
│   │   │   ├── common/            # 通用组件
│   │   │   └── business/          # 业务组件
│   │   ├── services/              # API 服务
│   │   ├── store/                 # 状态管理
│   │   ├── utils/                 # 工具函数
│   │   ├── types/                 # TypeScript 类型定义
│   │   ├── hooks/                 # 自定义 Hooks
│   │   └── constants/             # 常量定义
│   ├── public/
│   └── package.json
│
├── backend/                       # 后端项目
│   ├── src/
│   │   ├── modules/               # 业务模块
│   │   │   ├── auth/              # 认证模块
│   │   │   ├── tenant/            # 租户管理
│   │   │   ├── skill/             # 知识库
│   │   │   ├── agent/             # 智能体控制
│   │   │   ├── dashboard/         # 数据看板
│   │   │   ├── design/            # 设计工作台
│   │   │   └── competitor/        # 竞品监测
│   │   ├── common/                # 公共模块
│   │   │   ├── middleware/
│   │   │   ├── guards/
│   │   │   ├── interceptors/
│   │   │   ├── filters/
│   │   │   └── decorators/
│   │   ├── database/              # 数据库相关
│   │   │   ├── migrations/
│   │   │   ├── seeds/
│   │   │   └── entities/
│   │   ├── config/                # 配置
│   │   └── utils/                 # 工具函数
│   ├── test/                      # 测试
│   │   ├── unit/
│   │   ├── integration/
│   │   └── e2e/
│   └── package.json
│
├── shared/                        # 前后端共享
│   ├── types/                     # 共享类型定义
│   ├── constants/                 # 常量
│   └── schemas/                   # Schema 定义
│
├── docs/                          # 文档
│   ├── api/                       # API 文档
│   ├── architecture/              # 架构文档
│   ├── development/               # 开发文档
│   ├── deployment/                # 部署文档
│   └── dev-logs/                  # 开发日志
│       └── 2026-09/
│
├── scripts/                       # 脚本
│   ├── dev/                       # 开发脚本
│   ├── build/                     # 构建脚本
│   ├── deploy/                    # 部署脚本
│   └── backup/                    # 备份脚本
│
├── .github/                       # GitHub 配置
│   ├── workflows/                 # CI/CD
│   ├── PULL_REQUEST_TEMPLATE.md
│   └── ISSUE_TEMPLATE/
│
├── docker/                        # Docker 配置
│   ├── frontend.Dockerfile
│   ├── backend.Dockerfile
│   └── docker-compose.yml
│
├── .gitignore
├── .editorconfig
├── .prettierrc
├── .eslintrc.js
├── README.md
├── 产品开发方案.md                 # 产品方案文档
└── 开发规范文档.md                 # 本文档
```

### 2.2 命名规范

#### 文件命名

```
# 组件文件 - PascalCase
DashboardCard.tsx
AgentControlPanel.tsx
SkillEditor.vue

# 工具函数文件 - camelCase
formatDate.ts
parseSkillYaml.ts
validateInput.ts

# 常量文件 - UPPER_SNAKE_CASE
API_ROUTES.ts
ERROR_CODES.ts
DEFAULT_CONFIG.ts

# 页面路由文件 - kebab-case
merchant-management.tsx
skill-library.tsx
agent-control.tsx
```

#### 变量命名

```typescript
// 常量 - UPPER_SNAKE_CASE
const MAX_RETRY_COUNT = 3;
const API_BASE_URL = 'https://api.example.com';
const DEFAULT_PAGE_SIZE = 20;

// 变量 - camelCase
const userName = 'admin';
const aiAnalysisResult = {};
const currentTenant = null;

// 类/接口 - PascalCase
class AgentConnector {}
interface SkillMetadata {}
type UserRole = 'admin' | 'merchant';

// 私有属性 - _camelCase
private _internalState = {};
private _connectionPool = null;

// 布尔值 - is/has/should/can 前缀
const isLoading = true;
const hasPermission = false;
const shouldRetry = true;
const canEdit = false;

// 数组 - 复数形式
const users = [];
const skills = [];
const merchants = [];
```

#### API 路由命名

```
# RESTful 风格
GET    /api/v1/tenants              # 获取租户列表
POST   /api/v1/tenants              # 创建租户
GET    /api/v1/tenants/:id          # 获取单个租户
PUT    /api/v1/tenants/:id          # 更新租户
PATCH  /api/v1/tenants/:id          # 部分更新租户
DELETE /api/v1/tenants/:id          # 删除租户

# 嵌套资源
GET    /api/v1/tenants/:id/shops    # 获取租户的店铺列表
POST   /api/v1/tenants/:id/shops    # 为租户创建店铺

# 操作动词（非标准 RESTful）
POST   /api/v1/skills/:id/download  # 下载 Skill
POST   /api/v1/agents/:id/execute   # 执行智能体任务
POST   /api/v1/analysis/generate    # 生成分析报告
```

### 2.3 代码风格

#### TypeScript/JavaScript

```typescript
// ✅ 好的代码
/**
 * 解析 Skill YAML 文件
 * @param yamlContent - YAML 文件内容
 * @returns 解析后的 Skill 对象
 * @throws {InvalidSkillFormatError} 当 YAML 格式不正确时
 */
export function parseSkillYaml(yamlContent: string): Skill {
  try {
    const parsed = yaml.parse(yamlContent);
    
    // 验证必填字段
    if (!parsed.name || !parsed.description) {
      throw new InvalidSkillFormatError('缺少必填字段: name 或 description');
    }
    
    // 返回标准化对象
    return {
      name: parsed.name,
      description: parsed.description,
      version: parsed.version || '1.0',
      steps: parsed.steps || [],
      inputs: parsed.inputs || [],
      outputs: parsed.outputs || [],
    };
  } catch (error) {
    logger.error('解析 Skill YAML 失败', { 
      error: error.message, 
      yamlContent: yamlContent.substring(0, 100) // 只记录前100字符
    });
    throw error;
  }
}

// ❌ 不好的代码
function parse(str) {  // 函数名不明确，参数名太短
  let r = yaml.parse(str);  // 变量名不清晰
  return r;  // 缺少错误处理和验证
}
```

#### 代码注释规范

```typescript
// 单行注释: 解释 "为什么" 而不是 "是什么"
// ✅ 好的注释
// 使用 setTimeout 而不是 setInterval，避免任务堆积
setTimeout(checkStatus, 5000);

// ❌ 不好的注释
// 设置定时器
setTimeout(checkStatus, 5000);

// 多行注释: JSDoc 格式
/**
 * 执行智能体任务
 * 
 * @param taskId - 任务唯一标识
 * @param skillId - 关联的 Skill ID
 * @param params - 任务参数
 * @returns Promise<TaskResult> 任务执行结果
 * @throws {AgentNotConnectedError} 当智能体未连接时
 * @throws {TaskExecutionError} 当任务执行失败时
 * 
 * @example
 * ```typescript
 * const result = await executeAgentTask('task_123', 'skill_001', {
 *   product_id: 'prod_001',
 *   platform: 'taobao'
 * });
 * ```
 */
async function executeAgentTask(
  taskId: string, 
  skillId: string, 
  params: TaskParams
): Promise<TaskResult> {
  // 实现代码
}
```

#### 配置文件注释

```typescript
// config/app.config.ts
export default {
  // ========== 应用基础配置 ==========
  app: {
    name: '电商智能运营中台',
    version: '1.0.0',
    port: process.env.PORT || 3000,
    env: process.env.NODE_ENV || 'development',
  },
  
  // ========== 数据库配置 ==========
  database: {
    host: process.env.DB_HOST || 'localhost',
    port: parseInt(process.env.DB_PORT || '5432'),
    database: process.env.DB_NAME || 'ecommerce_platform',
    username: process.env.DB_USER || 'postgres',
    // ⚠️ 生产环境必须使用环境变量
    password: process.env.DB_PASSWORD,
    // 连接池配置
    pool: {
      min: 2,
      max: 10,
    },
  },
  
  // ========== AI 服务配置 ==========
  ai: {
    provider: 'claude',
    // ⚠️ 敏感信息，必须使用环境变量
    apiKey: process.env.CLAUDE_API_KEY,
    model: 'claude-sonnet-5',
    maxTokens: 4096,
    maxRetries: 3,  // API 调用失败重试次数
    timeout: 30000, // 30秒超时
  },
  
  // ========== Redis 配置 ==========
  redis: {
    host: process.env.REDIS_HOST || 'localhost',
    port: parseInt(process.env.REDIS_PORT || '6379'),
    password: process.env.REDIS_PASSWORD,
    db: 0,
    keyPrefix: 'ecommerce:', // 所有key添加前缀
  },
};
```

### 2.4 代码质量要求

#### ESLint 配置

```javascript
// .eslintrc.js
module.exports = {
  parser: '@typescript-eslint/parser',
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react/recommended',
    'prettier',
  ],
  rules: {
    // 禁止使用 console（警告级别，生产环境应移除）
    'no-console': 'warn',
    
    // 禁止使用 any 类型
    '@typescript-eslint/no-explicit-any': 'error',
    
    // 未使用的变量（错误级别）
    '@typescript-eslint/no-unused-vars': 'error',
    
    // 要求明确的函数返回类型
    '@typescript-eslint/explicit-function-return-type': 'warn',
    
    // 禁止 var，使用 let/const
    'no-var': 'error',
    
    // 优先使用 const
    'prefer-const': 'error',
  },
};
```

#### Prettier 配置

```json
// .prettierrc
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100,
  "arrowParens": "always"
}
```

---

## 三、开发日志规范

### 3.1 日志记录工具

**后端日志**: Winston (Node.js)

```bash
npm install winston winston-daily-rotate-file
```

**前端日志**: console 封装 + Sentry（错误监控）

```bash
npm install @sentry/react
```

### 3.2 日志级别定义

| 级别 | 说明 | 使用场景 |
|------|------|----------|
| ERROR | 错误 | 系统异常、第三方服务失败、数据库错误 |
| WARN | 警告 | 不影响功能但需要关注的问题 |
| INFO | 信息 | 关键业务流程记录、API调用 |
| DEBUG | 调试 | 开发环境详细信息 |
| TRACE | 追踪 | 最详细的日志（性能分析） |

### 3.3 日志格式规范

```typescript
// logger.config.ts
import winston from 'winston';
import DailyRotateFile from 'winston-daily-rotate-file';

const logFormat = winston.format.combine(
  winston.format.timestamp({ format: 'YYYY-MM-DD HH:mm:ss.SSS' }),
  winston.format.errors({ stack: true }),
  winston.format.json()
);

export const logger = winston.createLogger({
  level: process.env.LOG_LEVEL || 'info',
  format: logFormat,
  defaultMeta: {
    service: 'ecommerce-platform',
    environment: process.env.NODE_ENV,
  },
  transports: [
    // 控制台输出
    new winston.transports.Console({
      format: winston.format.combine(
        winston.format.colorize(),
        winston.format.simple()
      ),
    }),
    
    // 错误日志单独存储
    new DailyRotateFile({
      filename: 'logs/error-%DATE%.log',
      datePattern: 'YYYY-MM-DD',
      level: 'error',
      maxSize: '20m',
      maxFiles: '30d',  // 保留 30 天
    }),
    
    // 所有日志
    new DailyRotateFile({
      filename: 'logs/combined-%DATE%.log',
      datePattern: 'YYYY-MM-DD',
      maxSize: '50m',
      maxFiles: '14d',  // 保留 14 天
    }),
  ],
});
```

### 3.4 日志记录最佳实践

```typescript
import { logger } from './logger.config';

// ✅ 1. API 请求日志
app.use((req, res, next) => {
  logger.info('API请求', {
    method: req.method,
    url: req.url,
    tenantId: req.user?.tenantId,
    userId: req.user?.id,
    ip: req.ip,
    userAgent: req.headers['user-agent'],
  });
  next();
});

// ✅ 2. 业务流程日志
async function executeAgentTask(taskId: string, skillId: string) {
  logger.info('开始执行智能体任务', { taskId, skillId });
  
  try {
    const startTime = Date.now();
    const result = await agentConnector.execute(taskId);
    const duration = Date.now() - startTime;
    
    logger.info('智能体任务执行成功', {
      taskId,
      skillId,
      duration,
      resultSummary: result.summary,  // 不记录完整结果
    });
    
    return result;
  } catch (error) {
    logger.error('智能体任务执行失败', {
      taskId,
      skillId,
      error: error.message,
      stack: error.stack,
    });
    throw error;
  }
}

// ✅ 3. 性能日志
function logPerformance(operation: string, duration: number) {
  if (duration > 1000) {
    logger.warn('操作耗时过长', { operation, duration });
  } else {
    logger.debug('操作性能', { operation, duration });
  }
}

// ✅ 4. 敏感信息脱敏
function maskEmail(email: string): string {
  const [name, domain] = email.split('@');
  return `${name[0]}***@${domain}`;
}

logger.info('用户登录', {
  userId: user.id,
  email: maskEmail(user.email),  // u***@example.com
  ip: req.ip,
  // ❌ 不要记录: password, token, apiKey
});

// ❌ 5. 避免的做法
// 不要在生产环境使用 console.log
console.log('这是一条日志'); // ❌

// 不要记录完整的大对象
logger.info('数据', { data: hugeObject }); // ❌

// 不要记录敏感信息
logger.info('用户信息', { password: '123456' }); // ❌
```

### 3.5 日志文件组织

```
logs/
├── error-2026-09-16.log        # 错误日志
├── combined-2026-09-16.log     # 所有日志
├── access-2026-09-16.log       # 访问日志（Nginx 格式）
├── agent-tasks-2026-09-16.log  # 智能体任务日志
└── ai-calls-2026-09-16.log     # AI API 调用日志
```

### 3.6 个人开发日志模板

每个开发者在 `docs/dev-logs/` 目录下维护个人日志：

```markdown
<!-- docs/dev-logs/2026-09/张三-0916.md -->

# 开发日志 - 张三 - 2026-09-16

## 今日任务

- [ ] 完成数据看板 AI 分析建议 UI
- [ ] 修复 Bug #123: 价格计算错误
- [ ] Code Review: PR #45

## 完成情况

### 1. 数据看板 AI 分析建议 UI

**分支**: `feature/dashboard-ai-analysis`  
**完成度**: 80%  
**提交记录**:
- `feat(dashboard): 添加建议卡片组件` (commit: a1b2c3d)
- `feat(dashboard): 实现优先级颜色标识` (commit: e4f5g6h)

**问题**:
- 执行按钮交互逻辑复杂，需要明天继续
- 与后端 API `/api/v1/analysis/suggestions` 对接有问题

**明天计划**: 完成执行按钮交互 + 联调后端 API

### 2. Bug #123: 价格计算错误

**状态**: ✅ 已修复  
**分支**: `bugfix/#123-fix-price-calculation`  
**问题原因**: 计算折扣时未考虑四舍五入  
**解决方案**: 使用 `Decimal` 库进行精确计算  
**PR**: #50（待审核）

### 3. Code Review

- **PR #45**: `feature/skill-download` - 已通过，提出 3 条优化建议
- **PR #48**: `bugfix/sql-injection` - 发现安全问题，要求修改

## 遇到的问题

### 问题 1: Claude API 调用偶尔超时

**现象**: 部分请求超过 30 秒无响应  
**临时方案**: 增加重试逻辑  
**待讨论**: 是否需要加入队列机制

### 问题 2: 前端状态管理混乱

**建议**: 引入 Redux Toolkit 替换 Zustand  
**需要**: 团队讨论

## 学习与思考

- 学习了 PostgreSQL JSONB 索引优化
- 阅读了 Codex JSON-RPC 文档
- 思考如何优化任务调度性能

## 明天计划

1. 完成数据看板 AI 分析建议功能
2. 参与技术评审会议（14:00）
3. 开始设计工作台模块开发

## 工作时长

- 9:00 - 12:00: 开发 (3小时)
- 14:00 - 18:00: 开发 + Code Review (4小时)
- **总计**: 7 小时
```

---

## 四、数据库变更管理

### 4.1 迁移脚本命名

```bash
# 使用时间戳 + 描述
YYYYMMDDHHMMSS_description.sql

# 示例
20260916120000_add_competitor_table.sql
20260916143000_add_index_on_metrics.sql
```

### 4.2 迁移文件模板

```sql
-- migrations/20260916120000_add_competitor_table.sql

-- ============================================
-- 迁移说明: 添加竞品监测表
-- 创建人: 张三
-- 创建时间: 2026-09-16
-- 关联需求: #TASK-123
-- ============================================

-- ============================================
-- 向上迁移 (UP)
-- ============================================
BEGIN;

-- 1. 创建竞品表
CREATE TABLE competitors (
  id SERIAL PRIMARY KEY,
  tenant_id INT NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
  my_product_id INT REFERENCES products(id) ON DELETE CASCADE,
  competitor_name VARCHAR(200) NOT NULL,
  competitor_url VARCHAR(500) NOT NULL,
  platform VARCHAR(20) NOT NULL,
  monitored_metrics JSONB DEFAULT '[]'::jsonb,
  status VARCHAR(20) DEFAULT 'active',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 2. 创建索引
CREATE INDEX idx_competitors_tenant ON competitors(tenant_id);
CREATE INDEX idx_competitors_product ON competitors(my_product_id);
CREATE INDEX idx_competitors_status ON competitors(status);
CREATE INDEX idx_competitors_platform ON competitors(platform);

-- 3. 添加注释
COMMENT ON TABLE competitors IS '竞品监测表';
COMMENT ON COLUMN competitors.tenant_id IS '租户ID（商家）';
COMMENT ON COLUMN competitors.monitored_metrics IS '监测指标: ["price", "sales", "rating"]';

-- 4. 添加触发器（自动更新 updated_at）
CREATE TRIGGER update_competitors_updated_at
  BEFORE UPDATE ON competitors
  FOR EACH ROW
  EXECUTE FUNCTION update_updated_at_column();

COMMIT;

-- ============================================
-- 向下迁移 (DOWN)
-- 保存到: migrations/20260916120000_add_competitor_table.down.sql
-- ============================================
-- BEGIN;
-- DROP TRIGGER IF EXISTS update_competitors_updated_at ON competitors;
-- DROP TABLE IF EXISTS competitors CASCADE;
-- COMMIT;
```

### 4.3 数据库变更检查清单

提交迁移脚本前，必须确认：

```markdown
## 数据库变更检查清单

### 脚本质量
- [ ] 迁移脚本包含清晰的注释说明
- [ ] 使用 BEGIN/COMMIT 包裹事务
- [ ] 已创建必要的索引
- [ ] 外键约束正确设置（ON DELETE/UPDATE）
- [ ] 已添加表和字段注释
- [ ] 已编写回滚脚本 (.down.sql)

### 测试验证
- [ ] 在本地数据库测试通过
- [ ] 在测试数据上验证性能
- [ ] 测试了回滚脚本
- [ ] 验证了数据迁移逻辑（如果有）

### 代码同步
- [ ] 已更新 ORM 模型文件
- [ ] 已更新 TypeScript 类型定义
- [ ] 已更新 API 文档
- [ ] 已更新数据库设计文档

### 团队沟通
- [ ] 已通知团队成员
- [ ] 重要变更已在会议中讨论
```

### 4.4 数据库操作规范

```sql
-- ✅ 好的 SQL
-- 1. 使用参数化查询（防止 SQL 注入）
SELECT * FROM tenants WHERE id = $1;

-- 2. 合理使用索引
SELECT * FROM metrics 
WHERE tenant_id = $1 AND date >= $2 
ORDER BY date DESC 
LIMIT 100;

-- 3. 避免 SELECT *，明确字段
SELECT id, name, status, created_at 
FROM tenants 
WHERE status = 'active';

-- 4. 使用事务包裹多个操作
BEGIN;
INSERT INTO tenants (...) VALUES (...);
INSERT INTO shops (...) VALUES (...);
COMMIT;

-- ❌ 不好的 SQL
-- 1. 字符串拼接（SQL 注入风险）
SELECT * FROM tenants WHERE id = '${userId}';  -- ❌

-- 2. 没有分页的全表查询
SELECT * FROM metrics;  -- ❌

-- 3. N+1 查询问题
-- 应该使用 JOIN 一次查询
for (const tenant of tenants) {
  const shops = await db.query('SELECT * FROM shops WHERE tenant_id = $1', [tenant.id]);
}
```

---

## 五、测试规范

### 5.1 测试覆盖率要求

| 类型 | 覆盖率要求 |
|------|-----------|
| 核心业务逻辑 | ≥ 80% |
| API 接口 | ≥ 70% |
| 工具函数 | ≥ 90% |
| UI 组件 | ≥ 60% |

### 5.2 测试文件组织

```
backend/
├── src/
│   └── modules/
│       └── skill/
│           ├── skill.service.ts
│           ├── skill.service.spec.ts        # 单元测试
│           ├── skill.controller.ts
│           └── skill.controller.spec.ts     # 单元测试
└── test/
    ├── unit/                                # 单元测试
    ├── integration/                         # 集成测试
    │   └── skill.e2e.spec.ts
    ├── e2e/                                 # 端到端测试
    └── fixtures/                            # 测试数据
        ├── skill-sample.yaml
        └── mock-data.json
```

### 5.3 测试命名规范

```typescript
describe('SkillService', () => {
  describe('parseSkillYaml', () => {
    it('应该成功解析有效的 YAML 文件', () => {
      // Arrange
      const yamlContent = '...';
      
      // Act
      const result = parseSkillYaml(yamlContent);
      
      // Assert
      expect(result.name).toBe('商品标题优化');
    });
    
    it('应该在缺少必填字段时抛出错误', () => {
      const invalidYaml = 'name: test'; // 缺少 description
      
      expect(() => parseSkillYaml(invalidYaml))
        .toThrow(InvalidSkillFormatError);
    });
    
    it('应该为可选字段提供默认值', () => {
      const minimalYaml = 'name: test\ndescription: desc';
      const result = parseSkillYaml(minimalYaml);
      
      expect(result.version).toBe('1.0');
      expect(result.steps).toEqual([]);
    });
  });
});
```

### 5.4 测试最佳实践

```typescript
// ✅ 好的测试
describe('AgentTaskService', () => {
  let service: AgentTaskService;
  let mockAgentConnector: jest.Mocked<AgentConnector>;
  
  beforeEach(() => {
    // 每个测试前重置
    mockAgentConnector = {
      execute: jest.fn(),
      getStatus: jest.fn(),
    } as any;
    
    service = new AgentTaskService(mockAgentConnector);
  });
  
  it('应该成功执行任务并返回结果', async () => {
    // Arrange
    const taskId = 'task_123';
    const expectedResult = { status: 'completed' };
    mockAgentConnector.execute.mockResolvedValue(expectedResult);
    
    // Act
    const result = await service.executeTask(taskId);
    
    // Assert
    expect(result).toEqual(expectedResult);
    expect(mockAgentConnector.execute).toHaveBeenCalledWith(taskId);
    expect(mockAgentConnector.execute).toHaveBeenCalledTimes(1);
  });
  
  it('应该在智能体未连接时抛出错误', async () => {
    mockAgentConnector.execute.mockRejectedValue(
      new AgentNotConnectedError('Agent offline')
    );
    
    await expect(service.executeTask('task_123'))
      .rejects
      .toThrow(AgentNotConnectedError);
  });
});

// ❌ 不好的测试
it('测试功能', () => {  // 名称不清晰
  const result = func();  // 没有 Arrange/Act/Assert 结构
  expect(result).toBeTruthy();  // 断言不具体
});
```

---

## 六、Code Review 规范

### 6.1 Pull Request 模板

创建 `.github/PULL_REQUEST_TEMPLATE.md`:

```markdown
## 变更说明
<!-- 简要描述本次 PR 的目的和主要变更 -->

## 变更类型
- [ ] 新功能 (feature)
- [ ] Bug 修复 (bugfix)
- [ ] 重构 (refactor)
- [ ] 性能优化 (perf)
- [ ] 文档更新 (docs)
- [ ] 测试相关 (test)
- [ ] 其他

## 关联 Issue
<!-- 关联的 Issue 编号，例如: Closes #123, Relates to #456 -->

## 变更内容
<!-- 详细列出本次变更的内容 -->
- 
- 
- 

## 测试情况
- [ ] 已添加单元测试
- [ ] 已添加集成测试
- [ ] 已在本地测试通过
- [ ] 已在测试环境验证

## 截图/GIF（如果适用）
<!-- 添加截图或 GIF 演示，特别是 UI 变更 -->

## 数据库变更
- [ ] 无数据库变更
- [ ] 已包含迁移脚本
- [ ] 已包含回滚脚本
- [ ] 已在本地测试迁移

## 检查清单
- [ ] 代码符合项目规范
- [ ] 已更新相关文档
- [ ] 没有遗留 console.log / debugger / TODO
- [ ] 已处理所有 ESLint 警告
- [ ] 已添加必要的注释
- [ ] API 变更已更新接口文档
- [ ] 敏感信息已使用环境变量

## 性能影响
<!-- 如果有性能影响，请说明 -->
- [ ] 无性能影响
- [ ] 性能提升
- [ ] 可能的性能影响（已说明）

## 安全性检查
- [ ] 无安全风险
- [ ] 已处理 SQL 注入风险
- [ ] 已处理 XSS 风险
- [ ] 敏感数据已脱敏

## 其他说明
<!-- 需要特别说明的内容 -->
```

### 6.2 Code Review 检查项

审核者必须检查以下内容：

#### 功能性
- [ ] 代码实现了 PR 描述的功能
- [ ] 边界条件处理正确
- [ ] 错误处理完善
- [ ] 输入验证充分

#### 代码质量
- [ ] 命名清晰易懂（变量、函数、类）
- [ ] 逻辑简洁，无过度复杂
- [ ] 无重复代码（DRY 原则）
- [ ] 注释恰当（解释 why，不是 what）
- [ ] 函数职责单一
- [ ] 代码可读性好

#### 安全性
- [ ] 无 SQL 注入风险
- [ ] 无 XSS 风险
- [ ] 无 CSRF 风险
- [ ] 敏感信息已脱敏或加密
- [ ] 权限检查正确
- [ ] 输入验证完整

#### 性能
- [ ] 无 N+1 查询问题
- [ ] 数据库查询有索引支持
- [ ] 大数据量处理有分页
- [ ] 无内存泄漏风险
- [ ] 合理使用缓存

#### 测试
- [ ] 测试用例覆盖主要场景
- [ ] 测试用例可读性好
- [ ] 边界条件有测试
- [ ] 错误情况有测试

#### 文档
- [ ] API 变更已更新文档
- [ ] 复杂逻辑有注释说明
- [ ] README 已更新（如果需要）

### 6.3 Code Review 评论规范

```markdown
# ✅ 好的评论

## 必须修改 (Blocking)
🔴 **必须修改**: 这里存在 SQL 注入风险，必须使用参数化查询。

```typescript
// 不安全 ❌
const sql = `SELECT * FROM users WHERE id = '${userId}'`;

// 安全 ✅
const sql = 'SELECT * FROM users WHERE id = $1';
const result = await db.query(sql, [userId]);
```

## 建议优化 (Non-blocking)
🟡 **建议**: 可以将这个函数拆分成更小的函数，提高可读性。

```typescript
// 当前代码
function processOrder(order) {
  // 100 行代码...
}

// 建议优化
function processOrder(order) {
  validateOrder(order);
  calculatePrice(order);
  updateInventory(order);
  sendNotification(order);
}
```

## 疑问
🔵 **疑问**: 这里为什么使用 setTimeout 而不是 setInterval？能否解释一下原因？

## 赞赏
🟢 **很好**: 这个错误处理很完善，考虑得很周到！

# ❌ 不好的评论
这段代码不好。  // 太笼统，没有说明什么不好
改一下。  // 没有给出建议
```

---

## 七、CI/CD 流程

### 7.1 GitHub Actions 配置

创建 `.github/workflows/ci.yml`:

```yaml
name: CI/CD Pipeline

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  # ========== 代码检查 ==========
  lint:
    name: 代码规范检查
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run ESLint
        run: npm run lint
      
      - name: Run Prettier check
        run: npm run format:check
  
  # ========== 单元测试 ==========
  test:
    name: 单元测试
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests with coverage
        run: npm run test:coverage
      
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/coverage-final.json
          fail_ci_if_error: true
  
  # ========== 构建 ==========
  build:
    name: 构建项目
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build project
        run: npm run build
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: build
          path: dist/
  
  # ========== 部署到测试环境 ==========
  deploy-staging:
    name: 部署到测试环境
    runs-on: ubuntu-latest
    needs: [build]
    if: github.ref == 'refs/heads/develop'
    environment: staging
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Download build artifacts
        uses: actions/download-artifact@v3
        with:
          name: build
          path: dist/
      
      - name: Deploy to staging
        run: |
          echo "部署到测试环境"
          # 部署脚本
          ./scripts/deploy/deploy-staging.sh
  
  # ========== 部署到生产环境 ==========
  deploy-production:
    name: 部署到生产环境
    runs-on: ubuntu-latest
    needs: [build]
    if: github.ref == 'refs/heads/main'
    environment: production  # 需要手动批准
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Download build artifacts
        uses: actions/download-artifact@v3
        with:
          name: build
          path: dist/
      
      - name: Deploy to production
        run: |
          echo "部署到生产环境"
          # 部署脚本
          ./scripts/deploy/deploy-production.sh
```

### 7.2 部署脚本示例

```bash
#!/bin/bash
# scripts/deploy/deploy-staging.sh

set -e  # 遇到错误立即退出

echo "========== 开始部署到测试环境 =========="

# 1. 环境变量检查
if [ -z "$STAGING_HOST" ]; then
  echo "❌ 错误: 未设置 STAGING_HOST 环境变量"
  exit 1
fi

# 2. 构建 Docker 镜像
echo "构建 Docker 镜像..."
docker build -t ecommerce-platform:staging -f docker/backend.Dockerfile .

# 3. 推送镜像到仓库
echo "推送镜像到 Docker Registry..."
docker tag ecommerce-platform:staging $DOCKER_REGISTRY/ecommerce-platform:staging
docker push $DOCKER_REGISTRY/ecommerce-platform:staging

# 4. SSH 到测试服务器并部署
echo "部署到测试服务器..."
ssh $STAGING_HOST << 'EOF'
  cd /opt/ecommerce-platform
  docker-compose pull
  docker-compose up -d
  docker-compose logs -f --tail=100
EOF

# 5. 健康检查
echo "等待服务启动..."
sleep 10

HEALTH_CHECK_URL="https://staging.example.com/api/health"
HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" $HEALTH_CHECK_URL)

if [ $HTTP_CODE -eq 200 ]; then
  echo "✅ 部署成功！健康检查通过"
else
  echo "❌ 部署失败！健康检查未通过（HTTP $HTTP_CODE）"
  exit 1
fi

echo "========== 部署完成 =========="
```

---

## 八、备份与恢复策略

### 8.1 Git 代码备份

```bash
#!/bin/bash
# scripts/backup/backup-git.sh

BACKUP_REPO="git@backup-server:ecommerce-platform-backup.git"
BACKUP_DIR="./backups"
DATE=$(date +%Y%m%d)

echo "========== Git 备份开始 =========="

# 1. 创建备份目录
mkdir -p $BACKUP_DIR

# 2. 备份所有分支到远程备份仓库
echo "备份所有分支..."
git push --all $BACKUP_REPO

# 3. 备份所有标签
echo "备份所有标签..."
git push --tags $BACKUP_REPO

# 4. 创建本地归档
echo "创建本地归档..."
git bundle create "$BACKUP_DIR/repo-backup-$DATE.bundle" --all

# 5. 验证归档
echo "验证归档文件..."
git bundle verify "$BACKUP_DIR/repo-backup-$DATE.bundle"

# 6. 删除 30 天前的备份
echo "清理旧备份..."
find $BACKUP_DIR -name "*.bundle" -mtime +30 -delete

echo "✅ Git 备份完成: $DATE"
echo "备份文件: $BACKUP_DIR/repo-backup-$DATE.bundle"
```

### 8.2 数据库备份

```bash
#!/bin/bash
# scripts/backup/backup-database.sh

DB_NAME="ecommerce_platform"
DB_USER="postgres"
BACKUP_DIR="/var/backups/postgres"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/$DB_NAME-$DATE.sql.gz"

echo "========== 数据库备份开始 =========="

# 1. 创建备份目录
mkdir -p $BACKUP_DIR

# 2. 全量备份（带压缩）
echo "执行数据库备份..."
pg_dump -U $DB_USER -h localhost $DB_NAME | gzip > $BACKUP_FILE

# 3. 验证备份文件
if [ $? -eq 0 ] && [ -f $BACKUP_FILE ]; then
  FILE_SIZE=$(du -h $BACKUP_FILE | cut -f1)
  echo "✅ 数据库备份成功"
  echo "备份文件: $BACKUP_FILE"
  echo "文件大小: $FILE_SIZE"
else
  echo "❌ 数据库备份失败"
  exit 1
fi

# 4. 上传到云存储（可选）
if [ -n "$AWS_S3_BUCKET" ]; then
  echo "上传到 S3..."
  aws s3 cp $BACKUP_FILE s3://$AWS_S3_BUCKET/database/
fi

# 5. 删除 7 天前的本地备份
echo "清理旧备份..."
find $BACKUP_DIR -name "*.sql.gz" -mtime +7 -delete

echo "========== 数据库备份完成 =========="
```

### 8.3 数据库恢复

```bash
#!/bin/bash
# scripts/backup/restore-database.sh

BACKUP_FILE=$1

if [ -z "$BACKUP_FILE" ]; then
  echo "用法: ./restore-database.sh <备份文件路径>"
  exit 1
fi

if [ ! -f "$BACKUP_FILE" ]; then
  echo "❌ 错误: 备份文件不存在: $BACKUP_FILE"
  exit 1
fi

DB_NAME="ecommerce_platform"
DB_USER="postgres"

echo "========== 数据库恢复开始 =========="
echo "⚠️  警告: 此操作将覆盖现有数据库！"
read -p "确认恢复吗？(yes/no): " CONFIRM

if [ "$CONFIRM" != "yes" ]; then
  echo "取消恢复"
  exit 0
fi

# 1. 停止应用服务（避免连接冲突）
echo "停止应用服务..."
docker-compose stop backend

# 2. 断开所有数据库连接
echo "断开数据库连接..."
psql -U $DB_USER -c "SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname='$DB_NAME';"

# 3. 删除并重建数据库
echo "重建数据库..."
dropdb -U $DB_USER $DB_NAME
createdb -U $DB_USER $DB_NAME

# 4. 恢复数据
echo "恢复数据..."
gunzip -c $BACKUP_FILE | psql -U $DB_USER $DB_NAME

# 5. 验证恢复
echo "验证数据..."
TABLE_COUNT=$(psql -U $DB_USER -d $DB_NAME -t -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='public';")

if [ $TABLE_COUNT -gt 0 ]; then
  echo "✅ 数据库恢复成功"
  echo "表数量: $TABLE_COUNT"
else
  echo "❌ 数据库恢复失败"
  exit 1
fi

# 6. 重启应用服务
echo "重启应用服务..."
docker-compose start backend

echo "========== 数据库恢复完成 =========="
```

### 8.4 自动化备份任务

```bash
# 添加到 crontab
crontab -e

# 每天凌晨 2 点备份数据库
0 2 * * * /path/to/scripts/backup/backup-database.sh >> /var/log/backup.log 2>&1

# 每天凌晨 3 点备份 Git 仓库
0 3 * * * /path/to/scripts/backup/backup-git.sh >> /var/log/git-backup.log 2>&1

# 每周日凌晨 4 点执行完整备份（包括文件）
0 4 * * 0 /path/to/scripts/backup/backup-full.sh >> /var/log/full-backup.log 2>&1
```

---

## 九、文档规范

### 9.1 必备文档清单

```
docs/
├── README.md                          # 项目概述
├── GETTING_STARTED.md                 # 快速开始指南
├── CONTRIBUTING.md                    # 贡献指南
├── CHANGELOG.md                       # 变更日志
├── architecture/
│   ├── system-design.md               # 系统设计
│   ├── database-schema.md             # 数据库设计
│   ├── api-design.md                  # API 设计
│   └── tech-stack.md                  # 技术栈说明
├── api/
│   ├── api-overview.md                # API 概览
│   ├── authentication.md              # 认证说明
│   └── endpoints/                     # 各模块 API 文档
│       ├── tenant.md
│       ├── skill.md
│       ├── agent.md
│       ├── dashboard.md
│       └── competitor.md
├── development/
│   ├── setup.md                       # 开发环境搭建
│   ├── coding-standards.md            # 编码规范
│   ├── git-workflow.md                # Git 工作流
│   ├── testing.md                     # 测试指南
│   └── debugging.md                   # 调试指南
├── deployment/
│   ├── production-deployment.md       # 生产部署
│   ├── staging-deployment.md          # 测试环境部署
│   ├── backup-recovery.md             # 备份恢复
│   ├── monitoring.md                  # 监控运维
│   └── troubleshooting.md             # 故障排查
└── dev-logs/                          # 开发日志
    └── 2026-09/
```

### 9.2 CHANGELOG 格式

```markdown
# Changelog

所有重要的变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/)，
版本号遵循 [Semantic Versioning](https://semver.org/)。

## [Unreleased]

### Added
- 新增竞品监测功能
- 新增设计工作台模块
- 新增 AI 执行双模式（建议/确认）

### Changed
- 优化 AI 分析响应速度
- 改进智能体任务调度算法

### Fixed
- 修复价格计算精度问题 (#123)
- 修复数据看板加载缓慢 (#145)

### Security
- 修复 SQL 注入漏洞 (#100)

## [1.0.0] - 2026-09-16

### Added
- 数据看板基础功能
- 知识库管理系统
- 智能体控制中心
- 多租户支持
- 用户认证与权限系统

### Changed
- 使用 PostgreSQL 替换 MySQL

### Fixed
- 修复用户登录问题 (#12)

### Security
- 添加 CSRF 防护
```

---

## 十、开发规范检查清单

### 10.1 代码提交前检查清单

每次提交代码前，开发者必须自查：

```markdown
## 代码提交前检查清单

### Git 相关
- [ ] Commit 信息符合规范（type(scope): subject）
- [ ] 分支命名正确
- [ ] 已同步最新的 develop 分支
- [ ] 没有将敏感信息提交到仓库（密码、密钥）
- [ ] .gitignore 配置正确

### 代码质量
- [ ] 通过 ESLint 检查（npm run lint）
- [ ] 通过 Prettier 格式化（npm run format）
- [ ] 无 console.log、debugger 遗留
- [ ] 无 TODO 或已记录到 Issue
- [ ] 变量和函数命名清晰
- [ ] 复杂逻辑有注释说明

### 测试
- [ ] 已添加或更新测试用例
- [ ] 所有测试通过（npm run test）
- [ ] 测试覆盖率达标
- [ ] 边界条件有测试

### 功能验证
- [ ] 功能在本地测试通过
- [ ] 已测试错误情况
- [ ] 已测试边界条件
- [ ] UI 在主流浏览器测试（如果有）

### 文档
- [ ] 更新了相关文档
- [ ] 添加了必要的代码注释
- [ ] API 变更已更新 API 文档
- [ ] 重要变更已更新 CHANGELOG.md

### 数据库
- [ ] 无数据库变更（跳过）
- [ ] 数据库变更有迁移脚本
- [ ] 迁移脚本有回滚方案
- [ ] 已在本地测试迁移
- [ ] 已更新数据库设计文档

### 安全
- [ ] 敏感信息使用环境变量
- [ ] 用户输入已验证和过滤
- [ ] SQL 查询使用参数化
- [ ] 密码等敏感数据已加密
- [ ] API 有权限检查

### 性能
- [ ] 无明显性能问题
- [ ] 数据库查询有索引支持
- [ ] 大数据量有分页处理
- [ ] 合理使用缓存
```

### 10.2 Pull Request 提交前检查清单

```markdown
## Pull Request 提交前检查清单

### PR 信息
- [ ] PR 标题清晰描述变更
- [ ] PR 描述详细（使用模板）
- [ ] 关联了相关 Issue
- [ ] 标记了变更类型
- [ ] 添加了截图（如果是 UI 变更）

### 代码审查准备
- [ ] PR 大小合理（< 500 行）
- [ ] 变更聚焦单一功能
- [ ] 没有不相关的代码变更
- [ ] 自己先 Review 过一遍

### CI/CD
- [ ] CI/CD 流程全部通过
- [ ] 代码覆盖率未下降
- [ ] 构建成功

### 团队协作
- [ ] 指定了审核者
- [ ] 通知了相关团队成员
- [ ] 标记了优先级（如果紧急）
```

### 10.3 上线前检查清单

```markdown
## 上线前检查清单

### 代码准备
- [ ] 所有 PR 已合并到 main
- [ ] 代码已打 Tag（版本号）
- [ ] CHANGELOG 已更新

### 测试验证
- [ ] 在测试环境验证通过
- [ ] 回归测试通过
- [ ] 性能测试通过（如果有）
- [ ] 安全测试通过（如果有）

### 数据库
- [ ] 数据库迁移脚本已准备
- [ ] 迁移脚本在测试环境验证
- [ ] 准备了回滚脚本
- [ ] 数据备份已完成

### 配置
- [ ] 生产环境配置已检查
- [ ] 环境变量已设置
- [ ] 第三方服务密钥已配置

### 备份
- [ ] 数据库已备份
- [ ] 代码已备份
- [ ] 配置文件已备份

### 监控
- [ ] 监控告警已配置
- [ ] 日志收集正常
- [ ] 性能监控就绪

### 回滚准备
- [ ] 准备了回滚计划
- [ ] 回滚脚本已测试
- [ ] 明确回滚触发条件

### 团队沟通
- [ ] 已通知相关团队
- [ ] 安排了值班人员
- [ ] 准备了应急联系方式
```

---

## 附录

### A. 常用命令速查

> ⚠️ **本节的 Docker 命令与当前仓库实际情况不符（2026-09-17 核对）**：
> 1. compose 文件在 `docker/docker-compose.yml`，不在仓库根目录 —— 所有 `docker compose`
>    命令需加 `-f docker/docker-compose.yml`；
> 2. compose 只定义了 `postgres` / `redis` / `pgadmin` 三个服务，**没有 `backend` 服务** ——
>    后端目前在宿主机以 `npm run start:dev` 运行，`logs -f backend` / `restart backend` 均不可用。

```bash
# Git 操作
git checkout -b feature/new-feature  # 创建并切换到新分支
git push origin feature/new-feature  # 推送分支
git pull origin develop              # 拉取最新代码
git log --oneline -10                # 查看最近 10 条提交

# NPM 操作
npm ci                               # 安装依赖（CI 环境）
npm run lint                         # 代码检查
npm run format                       # 代码格式化
npm run test                         # 运行测试
npm run build                        # 构建项目

# Docker 操作
docker-compose up -d                 # 启动服务
docker-compose logs -f backend       # 查看后端日志
docker-compose restart backend       # 重启后端服务
docker-compose down                  # 停止所有服务

# 数据库操作
psql -U postgres -d ecommerce_platform  # 连接数据库
pg_dump -U postgres ecommerce_platform > backup.sql  # 备份
psql -U postgres ecommerce_platform < backup.sql     # 恢复
```

### B. 工具推荐

| 工具类别 | 推荐工具 | 用途 |
|---------|---------|------|
| IDE | VS Code, WebStorm | 代码编辑 |
| Git GUI | SourceTree, GitKraken | Git 可视化 |
| API 测试 | Postman, Insomnia | API 调试 |
| 数据库工具 | DBeaver, pgAdmin | 数据库管理 |
| 终端 | iTerm2, Windows Terminal | 命令行 |
| 文档 | Notion, Obsidian | 笔记和文档 |

### C. 学习资源

- [Git 工作流](https://www.atlassian.com/git/tutorials/comparing-workflows/gitflow-workflow)
- [TypeScript 最佳实践](https://www.typescriptlang.org/docs/handbook/declaration-files/do-s-and-don-ts.html)
- [Node.js 最佳实践](https://github.com/goldbergyoni/nodebestpractices)
- [React 最佳实践](https://react.dev/learn/thinking-in-react)

### D. 更新记录

| 版本 | 日期 | 更新内容 | 更新人 |
|------|------|----------|--------|
| v1.0 | 2026-09-16 | 初始版本，包含完整开发规范 | - |

---

**文档结束**

**重要提示**: 本文档是项目开发的重要依据，所有开发人员必须遵守。如有疑问或建议，请提 Issue 讨论。
